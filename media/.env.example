# =============================================================================
# Media Stack Environment Configuration
# =============================================================================
# Copy this file to .env and customize the values for your system.
# All values have sensible defaults - only change what you need.
#
# Usage:
#   cp .env.example .env
#   nano .env  # Edit values as needed
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: System User Configuration
# -----------------------------------------------------------------------------
# Get your user/group IDs by running: id $(whoami)
# Example output: uid=1000(user) gid=1000(user) ...

PUID=1000
PGID=1000
TZ=Australia/Brisbane

# -----------------------------------------------------------------------------
# REQUIRED: Data Directory
# -----------------------------------------------------------------------------
# Base directory for all media files. This should be a fast, large storage.
# Structure will be: DATA_DIR/{downloads,movies,shows,music,books}

DATA_DIR=/data

# -----------------------------------------------------------------------------
# OPTIONAL: Config Directory (Production Deployment)
# -----------------------------------------------------------------------------
# Directory for service configuration files (API keys, databases, etc.)
# 
# Default: . (current directory, same as compose.yaml)
# Production: /opt/docker/media or /var/lib/media-stack
#
# Using a separate config directory is recommended for production because:
# - Keeps git repo clean (no runtime files)
# - Easier backups (config separate from code)
# - Better security (API keys not near source code)
#
# Example production setup:
#   CONFIG_DIR=/opt/docker/media
#   # Creates: /opt/docker/media/sonarr, /opt/docker/media/radarr, etc.

CONFIG_DIR=.

# -----------------------------------------------------------------------------
# OPTIONAL: Network Configuration
# -----------------------------------------------------------------------------
# Custom Docker network subnet. Change only if 172.39.x.x conflicts with your network.

SERVARR_SUBNET=172.39.0.0/24

# Static IPs for services (used for inter-container communication)
IP_QBITTORRENT=172.39.0.2
IP_SONARR=172.39.0.3
IP_RADARR=172.39.0.4
IP_LIDARR=172.39.0.5
IP_BAZARR=172.39.0.6
IP_NZBGET=172.39.0.7
IP_PROWLARR=172.39.0.8
IP_FLARESOLVERR=172.39.0.9
IP_JELLYFIN=172.39.0.10
IP_JELLYSEERR=172.39.0.11

# -----------------------------------------------------------------------------
# OPTIONAL: Published Ports
# -----------------------------------------------------------------------------
# Change these if you have port conflicts on your host machine.

# Download clients
QBIT_WEBUI_PORT=8080
QBIT_TORRENT_PORT_TCP=6881
QBIT_TORRENT_PORT_UDP=6881
NZBGET_PORT=6789

# Arr stack
PROWLARR_PORT=9696
SONARR_PORT=8989
RADARR_PORT=7878
LIDARR_PORT=8686
BAZARR_PORT=6767
FLARESOLVERR_PORT=8191

# Media servers
JELLYFIN_PORT=8096
JELLYFIN_DISCOVERY_PORT=7359
JELLYSEERR_PORT=5055
JELLYSTAT_PORT=3000

# -----------------------------------------------------------------------------
# OPTIONAL: VPN Configuration (Gluetun)
# -----------------------------------------------------------------------------
# Enable VPN by running: docker compose --profile vpn up -d
# 
# These settings are for AirVPN with WireGuard. For other providers, see:
# https://github.com/qdm12/gluetun-wiki/tree/main/setup/providers

# VPN Provider settings
VPN_SERVICE_PROVIDER=airvpn
VPN_TYPE=wireguard

# AirVPN forwarded port (required for seeding)
# Get this from your AirVPN account: https://airvpn.org/ports/
FIREWALL_VPN_INPUT_PORTS=12345

# WireGuard keys (from AirVPN config generator)
# Generate at: https://airvpn.org/generator/
WIREGUARD_PUBLIC_KEY=your_public_key_here
WIREGUARD_PRIVATE_KEY=your_private_key_here
WIREGUARD_PRESHARED_KEY=your_preshared_key_here
WIREGUARD_ADDRESSES=10.x.x.x/32

# Server location (optional, comma-separated, no spaces)
SERVER_COUNTRIES=Australia
SERVER_CITIES=Sydney

# VPN health check timing
HEALTH_VPN_DURATION_INITIAL=120s

# -----------------------------------------------------------------------------
# OPTIONAL: Jellyfin Configuration
# -----------------------------------------------------------------------------
# Hardware transcoding device (uncomment if using Intel QuickSync)
# JELLYFIN_DEVICE=/dev/dri

# Jellystat database credentials
JELLYSTAT_DB_USER=postgres
JELLYSTAT_DB_PASS=jellystat_secure_password
JELLYSTAT_JWT_SECRET=change_this_to_a_random_string

# -----------------------------------------------------------------------------
# OPTIONAL: Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=info
LOG_HTML=false
CAPTCHA_SOLVER=none
